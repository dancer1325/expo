# "scripts/"

* == "reset-project.js"
  * if you want to run -> `npm run reset-project`
  * move the "src/" & "scripts/" -- to -- "/example" 
  * create a NEW "src/app/" / contains
    * "index.tsx"
    * "\_layout.tsx"
