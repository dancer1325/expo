# "src/components"

* == React Native components
  * _Example:_ "themed-text.tsx" creates text elements / use the app's color scheme | light & dark modes
  * types
    * platform-specific variants
      * 's naming
        * "*.native.tsx"
          * == native ones
        * "*.tsx"
          * == web ones
