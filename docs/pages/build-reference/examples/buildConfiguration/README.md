# steps
* | [here](buildConfiguration)
  * follow them
## AUTOMATICALLY create "eas.json" / default configuration
* [here](buildConfiguration/eas.json)
## if it's an Expo Project & NOT configured your "app.json" with `android.package` &/OR `ios.bundleIdentifier` -> EAS CLI will prompt you -- to -- specify them
TODO: create project for it