---
title: EAS CLI reference
sidebar_title: EAS CLI
description: EAS CLI is a command-line tool that allows you to interact with Expo Application Services (EAS) from your terminal.
cliVersion: 20.5.1
---

* EAS CLI
  * allows
    * | terminal window,
      * about workflows | your Expo & React Native project
      * build
      * update
      * submit
      * deploy
      * use  

## Installation

* ways
  * install the EAS CLI globally

    ```bash
    npm install --global eas-cli
    yarn global add eas-cli
    pnpm add --global eas-cli
    bun add --global eas-cli
    ```

  * provided -- by -- your package manager

    ```bash
    npx eas-cli@latest
    yarn dlx eas-cli@latest
    pnpm dlx eas-cli@latest
    bunx eas-cli@latest
    ```

## Commands

* TODO: check where to find them
<EASCLIReference />
